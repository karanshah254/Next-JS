// intercepting the about page using (...) followed by the route parameter or foldername
// for root directory for our example its "app" folder

export default function InterceptedAboutage() {
    return (
        <>
            <h1>
                Intercepted About Page using (...) followed by the folder name.
            </h1>
        </>
    )
}